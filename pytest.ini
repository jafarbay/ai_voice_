[pytest]
markers =
    slow: tests that load heavy models or do end-to-end inference (LLM, Whisper)
    integration: tests that hit real external resources or download data on first run
testpaths = tests
addopts = -ra
